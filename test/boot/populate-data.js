var loopback = require('loopback');

module.exports = function (app, done) {
  // var Literal = loopback.findModel('Literal');
  // var LiteralData = require('./data.json');

  // LiteralData.forEach(element => {
  //   Literal.create(element, {});
  // });
  done();
};

